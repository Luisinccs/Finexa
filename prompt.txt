Estoy intentando correr la aplicación Finexa/iOS/App/Finexa.ios.xcodeproj.
Cuando presiono el botón de + en el OperacionesController.swift, tengo este error en tiempo de ejecución:

---

Bien, ya funciona. Ahora tengo dos problemas inmediatos.
El combobox de la moneda hay que cargarlos con datos.
Los datos son contenidos en un vector como en el patrón que observas en 
Dual Components/DcBindings/Tests/Common/Include/ComboBoxTestViewModel:
using DcComboBoxItemList = std::vector<DcComboBoxItem>;
DcComboBoxItemList _listaMonedas;

Al final, se cargaran las monedas de la base de datos; por ahora, llena ese vector con tres monedas para prueba; puedes usar las mismas monedas que en el Finexa/Common/include/MonedasViewModel

El segundo problema es que al aceptar la creación de una operación, no se actiualiza la grilla, se debe mostrar tambien el total actualizado; reflejado en la moneda referencial.

---

Bien, tengo ahora este problema;
Agrega el primer registro (con problemas en las cifras, pero lo resolvemos despues); actualiza bien la lista de operaciones, pero cuando intentas crear un segundo registro, veo este error en tiempo de ejecución:

---

Bien, ya funciona, hay que hacer algunas correcciones en el formato y la lógica:
- Hay que colocar un selector para poder cambiar la moneda referencial para los cálculos. El actual lblReference.
- El monto total debe actualizarce (expresado en la moneda referencial) cada vez que se agrega, edita o elimina una operación. Y debe verse Total(Moneda): Monto; (ej Total(USD): 100.00)
- Hay un problema con las cifras reflejadas en la grilla; por ejemplo, si agrego 200$ en el formulario, se muestran 20000. 
- En el formulario para editar las operaciones, tambien hay un label que muestra el monto en moneda refernecial (Ahora no se actualizad) hay que hacer que muestre el monto